services:
  - type: web
    name: side-quest-app
    runtime: python
    plan: free
    buildCommand: pip install -r backend/requirements.txt
    startCommand: gunicorn backend.app:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
      - key: DATABASE_URL
        value: postgresql://postgres:4Qo32%27in2s%5DQ@db.uiojghbuvgedkoooudlc.supabase.co:5432/postgres
